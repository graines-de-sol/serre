.container
  #avatar
    = image_tag current_user.member.avatar.thumb('32x32').url

  #profil_link
    %span= hello
    = link_to current_user.member.first_name, accounts_path
    %span= "| Vous avez 2 nouveaux messages"

  = link_to t(:logout), destroy_user_session_path, :method => :delete, :id=>'logout'

