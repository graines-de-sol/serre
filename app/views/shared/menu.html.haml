.span2.show-grid.columns
  .tabbable.tabs-left
    %ul.nav.nav-tabs#main_menu
      - tabs.each do |tab|
        %li{:class=>('active' if params[:controller] == tab[:controller])}
          %a{:href=>"/#{tab[:controller]}"}
            %i{:class=>tab[:icon]}
            = t(tab[:name])

