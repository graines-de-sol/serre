%h3= "Prochains événements"
%hr
%ul.next_events
  - Event.upcoming.each do |event|
    %li
      = link_to event.title, events_path
      %br
      %span= l(event.start_at, :format => :long)

.clearfix
= link_to "Tous les événements", events_path, :class => 'readmore pull-right mt-10 mb-10'
.clearfix
