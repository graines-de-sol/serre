%h3= "Derniers partis"
%hr
- User.new_outers.each do |user|
  %a.avatar{:href=>member_path(user.member.id), :title => "#{user.member.first_name} #{user.member.last_name}", :data => {:do => 'tooltip'}}
    = avatar(user.member, '100x100')

.clearfix